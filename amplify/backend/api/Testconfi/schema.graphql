type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  Role: Role @hasOne
  email: String
  name: String!
  profileIUrlmage: String
}

type Role @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
}
 